# Screenshot Archive

This folder keeps the original screenshot inputs used before the public README images moved to polished generated result images.

- `current-project-assets/`: previous public README assets.
- `source-screenshots/`: plain iOS and macOS screenshots used as source material.

The public README uses generated result images from `assets/`.
